---
description: Launch the Agent-Company-X system on a goal (configurable harness, full autonomy, A2A board, veto loop, free models)
agent: orchestrator
---
Launch the Agent-Company-X system in goal mode. Load the skill first (skill tool, name `multi-agent-teams`), read the protocol at C:\Users\aariz\kilo_HQ\multi-agent-teams\protocol\TEAM_PROTOCOL.md, then execute the full loop until the goal is 100% met.

GOAL: $ARGUMENTS

Requirements: full autonomy (no permission prompts), free models only (see config/MODELS.md — spread across Mistral / Kilo :free / OpenCode Zen), A2A communication via the shared board (browser-hub_board_post/read), ONE shared browser (browser-hub MCP, per-agent tabs, never cross), verifier veto loop until 100% pass, then deliver with a full brief. CONFIGURE the team for this run (size, roles, tools, browser use, flow) — it is a harness, not a fixed roster. Scale down for small tasks, add swarms for big projects. Use the browser when the task needs it (mandatory if explicitly requested).
