---
description: Launch the multi-agent teams system on a goal (full autonomy, A2A, verification loop)
agent: orchestrator
---
Launch the Multi-Agent Teams system in goal mode. Load the skill first (skill tool, name `multi-agent-teams`), read the protocol at C:\Users\aariz\kilo_HQ\multi-agent-teams\protocol\TEAM_PROTOCOL.md, then execute the full loop until the goal is 100% met.

GOAL: $ARGUMENTS

Requirements: full autonomy (no permission prompts), A2A communication via the message board, Agent E verification veto loop until 100% pass, then deliver with a full brief. Scale the team to the task size. Use browsers when the task needs them (mandatory if explicitly requested).
